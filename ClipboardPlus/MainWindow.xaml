<Window x:Class="ClipboardPlus.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" xmlns:local="clr-namespace:ClipboardPlus"
 Title="Clipboard Plus" Style="{StaticResource {x:Type Window}}" Width="870" Height="760" MinWidth="760" MinHeight="580" WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize" PreviewKeyDown="Window_KeyDown">
 <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="54" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0"/></shell:WindowChrome.WindowChrome>
 <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="*"/><RowDefinition Height="36"/></Grid.RowDefinitions>
  <Grid Background="{DynamicResource SidebarBrush}">
   <StackPanel Orientation="Horizontal" Margin="20,0" VerticalAlignment="Center"><Image Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=Window}}" Width="30" Height="30"/><TextBlock Text="Clipboard Plus" Margin="10,0" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center"/><TextBlock Text="YOUR IDEAS, WITHIN REACH" Foreground="{DynamicResource MutedBrush}" FontSize="9" Margin="16,0" VerticalAlignment="Center"/></StackPanel>
   <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="8" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Button Content="—" Width="38" Padding="0" Background="Transparent" BorderThickness="0" Click="Minimize_Click" ToolTip="Minimize"/><Button Content="×" Width="38" Padding="0" Background="Transparent" BorderThickness="0" Click="Hide_Click" ToolTip="Hide to tray (Esc)"/></StackPanel>
  </Grid>
  <Grid Grid.Row="1">
   <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <Border Background="{DynamicResource SidebarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
    <DockPanel Margin="14,22,14,16">
     <StackPanel DockPanel.Dock="Bottom">
      <Border Padding="12" CornerRadius="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,0,16"><StackPanel><TextBlock Text="QUICK ACCESS" FontSize="9" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="ShortcutLabel" Text="Ctrl + Shift + V" Margin="0,7,0,0" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold"/></StackPanel></Border>
      <Button x:Name="PauseButton" Content="Ⅱ   Pause capture" Style="{StaticResource NavButton}" Click="Pause_Click"/>
      <Button Content="⚙   Settings" Style="{StaticResource NavButton}" Click="Settings_Click"/>
     </StackPanel>
     <StackPanel>
      <TextBlock Text="WORKSPACE" FontSize="10" Foreground="{DynamicResource MutedBrush}" Margin="14,0,0,14"/>
      <Button Content="▤   All history" Tag="All" Style="{StaticResource NavButton}" Click="Filter_Click"/>
      <Button Content="★   Pinned" Tag="Pinned" Style="{StaticResource NavButton}" Click="Filter_Click"/>
      <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="12,17"/>
      <TextBlock Text="CONTENT" FontSize="10" Foreground="{DynamicResource MutedBrush}" Margin="14,0,0,10"/>
      <Button Content="≡   Text &amp; code" Tag="Text" Style="{StaticResource NavButton}" Click="Filter_Click"/>
      <Button Content="▧   Images" Tag="Image" Style="{StaticResource NavButton}" Click="Filter_Click"/>
      <Button Content="↗   Links" Tag="Link" Style="{StaticResource NavButton}" Click="Filter_Click"/>
      <Button Content="▱   Files &amp; folders" Tag="Files" Style="{StaticResource NavButton}" Click="Filter_Click"/>
      <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="12,17"/>
      <Button Content="+   New snippet" Style="{StaticResource NavButton}" Click="Snippet_Click"/>
     </StackPanel>
    </DockPanel>
   </Border>
   <Grid Grid.Column="1" Margin="26,24,26,16">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Grid><StackPanel><TextBlock x:Name="Heading" Text="All history" FontSize="27" FontWeight="SemiBold"/><TextBlock Text="A little less searching. A lot more doing." Foreground="{DynamicResource MutedBrush}" Margin="0,6,0,0"/></StackPanel><Button Content="New snippet" HorizontalAlignment="Right" VerticalAlignment="Top" Click="Snippet_Click"/></Grid>
    <Grid Grid.Row="1" Margin="0,24,0,12"><TextBox x:Name="SearchBox" FontSize="15" Padding="14,12" TextChanged="Search_Changed" AutomationProperties.Name="Search clipboard history" ToolTip="Search words, filename, or source app. Ctrl+F to focus."/><TextBlock x:Name="SearchHint" Text="Search your clipboard…" IsHitTestVisible="False" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center" Margin="15,0"/></Grid>
    <DockPanel Grid.Row="2" Margin="0,0,0,12"><ComboBox x:Name="DateFilter" Width="130" DockPanel.Dock="Right" SelectionChanged="Date_Changed" SelectedIndex="0" AutomationProperties.Name="Date filter"><ComboBoxItem Content="Any time"/><ComboBoxItem Content="Today"/><ComboBoxItem Content="Last 7 days"/><ComboBoxItem Content="Last 30 days"/></ComboBox><Button x:Name="CopySelectedButton" Content="⧉  Copy (0)" Click="Copy_Click" DockPanel.Dock="Right" IsEnabled="False" Margin="8,0" Padding="9,5" ToolTip="Copy selected items together. The destination chooses the formats it supports." AutomationProperties.Name="Copy selected items"/><TextBlock x:Name="ResultLabel" Text="RECENT CLIPS" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center" FontSize="10"/></DockPanel>
    <Grid Grid.Row="3">
     <ListBox x:Name="ClipList" SelectionMode="Extended" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" PreviewMouseLeftButtonDown="Clip_PreviewMouseDown" MouseDoubleClick="Clip_DoubleClick" SelectionChanged="Selection_Changed" AutomationProperties.Name="Clipboard history">
      <ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="56"/><ColumnDefinition Width="*"/><ColumnDefinition Width="36"/></Grid.ColumnDefinitions><Border Width="44" Height="44" VerticalAlignment="Top" CornerRadius="7" Background="{DynamicResource BackgroundBrush}"><Grid><TextBlock Text="{Binding Icon}" FontSize="21" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource AccentBrush}"/><Button Tag="{Binding}" Click="Thumbnail_Click" Padding="0" BorderThickness="0" Background="Transparent" ToolTip="Open this image"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style><Image Source="{Binding Thumbnail}" Stretch="Uniform"/></Button></Grid></Border><StackPanel Grid.Column="1"><TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Preview}" Foreground="{DynamicResource MutedBrush}" Margin="0,7,0,10" TextTrimming="CharacterEllipsis" MaxHeight="35" TextWrapping="Wrap"/><TextBlock Text="{Binding Meta}" FontSize="10" Foreground="{DynamicResource MutedBrush}" TextTrimming="CharacterEllipsis"/><local:MediaControls Source="{Binding Summary.MediaSource}"/></StackPanel><StackPanel Grid.Column="2"><Button Content="⧉" Tag="{Binding}" Click="RowCopy_Click" Padding="4" Background="Transparent" BorderThickness="0" ToolTip="Copy this item" AutomationProperties.Name="Copy this item"/><Button Content="{Binding PinLabel}" Tag="{Binding}" VerticalAlignment="Top" Padding="4" Background="Transparent" BorderThickness="0" Click="Pin_Click" ToolTip="Pin or unpin item" Foreground="{DynamicResource AccentBrush}"/></StackPanel></Grid></DataTemplate></ListBox.ItemTemplate>
     </ListBox>
     <StackPanel x:Name="EmptyPanel" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"><TextBlock Text="▤" FontSize="50" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center"/><TextBlock x:Name="EmptyTitle" Text="Your next idea starts here" FontSize="20" Margin="0,18,0,8" HorizontalAlignment="Center"/><TextBlock x:Name="EmptyText" Text="Copy text, an image, or files. We’ll keep them within reach." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" MaxWidth="350" TextAlignment="Center"/></StackPanel>
    </Grid>
    <DockPanel Grid.Row="4" Margin="0,10,0,8"><Button x:Name="PreviousButton" Content="← Previous" Click="Previous_Click" DockPanel.Dock="Left"/><Button x:Name="NextButton" Content="Next →" Click="Next_Click" DockPanel.Dock="Right"/><TextBlock x:Name="PageLabel" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource MutedBrush}"/></DockPanel>
    <StackPanel Grid.Row="5">
     <WrapPanel Margin="0,4,0,8"><Button x:Name="PasteButton" Content="Paste  ↵" Style="{StaticResource AccentButton}" Click="Paste_Click" Margin="0,0,6,6"/><Button Content="Copy" Click="Copy_Click" Margin="0,0,6,6"/><Button Content="Preview" Click="Preview_Click" Margin="0,0,6,6"/><Button x:Name="RefreshButton" Content="↻ New clips" Visibility="Collapsed" Click="Refresh_Click" Margin="0,0,6,6" ToolTip="Refresh the list. New captures stay out of your way while you choose a clip."/><Button x:Name="PasteQueueButton" Content="Paste next queued" Visibility="Collapsed" Click="PasteQueue_Click" Margin="0,0,6,6"/><Button Content="Actions ▾" Click="Actions_Click" Margin="0,0,0,6"/></WrapPanel>
     <TextBlock x:Name="QueueLabel" Text="Enter to paste  ·  Ctrl+Enter for plain text  ·  Esc to hide" FontSize="10" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap"/>
    </StackPanel>
   </Grid>
  </Grid>
  <Border Grid.Row="2" Background="{DynamicResource SidebarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"><DockPanel Margin="18,0"><TextBlock x:Name="StorageLabel" DockPanel.Dock="Right" Text="Local &amp; private" Foreground="{DynamicResource MutedBrush}" FontSize="10" VerticalAlignment="Center"/><TextBlock x:Name="StatusLabel" Text="●  Ready to capture" Foreground="{DynamicResource AccentBrush}" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,12,0"/></DockPanel></Border>
 </Grid>
 </Border>
</Window>
